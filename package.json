{
  "name": "redish-sfa-workspace",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "packages/*",
    "frontend"
  ],
  "scripts": {
    "build": "npm run build --workspace=frontend",
    "vercel-build": "npm run build --workspace=frontend",
    "dev": "npm run dev --workspace=frontend",
    "lint": "npm run lint --workspace=frontend"
  },
  "devDependencies": {
    "csv-parse": "^6.1.0"
  }
}
