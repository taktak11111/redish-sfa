---
title: "プロジェクト再開ログ（REDISH_SFA）"
created: 2026-01-11 04:25
role: resume-log
---

## 目的
開発を最短で再開できる状態へ復帰する（Dev起動まで）。

## 再開サマリ
- 日時: 2026-01-11 04:25
- プロジェクト: REDISH_SFA
- 作業ディレクトリ: C:\Users\takta\Documents\develop\REDISH_SFA

## 前回の状態（既存ログより）
- 2025-12-24 07:34 に再開ログあり（`docs/00_Management/ResumeLogs/20251224_0734_プロジェクト再開.md`）
- 当時は Dev 起動（localhost:3000）と `.next` 再生成まで実施

## 今回の状態（現物確認）
### Git状態
- ブランチ: main（origin/mainと同期）
- 直近コミット: `5e9386f feat: SFA機能追加と改善 - 分析ページ追加、API改善、ドキュメント整備`
- 未コミット差分: あり（変更・削除・未追跡が混在）
  - 変更（例）: `frontend/src/app/(auth)/analysis/*`, `frontend/src/app/(auth)/*/page.tsx`, `frontend/src/types/sfa.ts` など
  - 削除: `frontend/src/components/shared/DateRangeFilter/*`（共通コンポーネントへ移行）
  - 未追跡: `frontend/src/app/api/call-lists/`, `frontend/src/components/calls/CallListCreateModal.tsx`, `docs/migrations/` など

### Dev稼働状況（端末ログより）
- `npm run dev -- -p 3002` が起動しており、`http://localhost:3002` でアクセス可能

### 型チェック
- `frontend/` で `npx tsc --noEmit` 実行: OK（エラー0）

## ブロッカー候補（要注意）
- `call_lists` テーブル未作成の場合、`/api/call-lists` が 500 になりうる（APIは明示的に案内メッセージを返す実装）
- `analysis/leads` は `deals.status` を参照しているため、DBスキーマと差分があるとエラーになりうる

## 次の3タスク（優先順）
1. 未コミット差分の整理方針を決める（WIPコミット / stash / 差分破棄のいずれか）
2. `call_lists` 系のDB整備（必要ならマイグレーション化して適用）
3. 分析機能（sales/leads/calls/field）のエラー有無を確認し、1つに絞って修正

## 開くべき主要ファイル
- `PROJECT_CONTROL.md`
- `docs/04_ルール・ガイドライン/開発ルール.md`
- `frontend/package.json`
- `frontend/next.config.js`
- `frontend/src/app/api/call-lists/route.ts`
- `frontend/src/app/api/analysis/leads/route.ts`
- `frontend/src/types/sfa.ts`

## Context Pack（コピー用）
> プロジェクト: REDISH_SFA  
> 目的: SFA β版の前進（分析/架電リストなど）  
> 現在地: mainで未コミット差分が大量にある状態  
> 次のタスク: 差分整理 → call_listsのDB整備 → 分析系の不具合を1つ潰す  
> Dev: `cd frontend; npm run dev -- -p 3002` → `http://localhost:3002`  

